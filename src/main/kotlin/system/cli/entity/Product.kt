package system.cli.entity

// Já chegou em data class?
//r:devo ter visto apenas não apliquei acabei esquecendo.

// Acho que nesse caso que só transita dados funciona bem
// e funciona muito bem quando faz um println na classe, já dá pra ver os valores legíveis
data class Product(val nome:String,val preco:Float)
